// Keep the product name in one place. Set VITE_APP_NAME at build time to
// change the application branding without editing individual screens.
export const APP_NAME = import.meta.env.VITE_APP_NAME || "CabLix";
